interface Split {
    transactionReference: string;
    cost: number;
    nicAmount: number;
    browncardAmount: number;
}

export const splitAmount = (input: Split) => {
    const totalAmount = input.cost;
    const nicAmount = input.nicAmount;
    const browncardAmount = input.browncardAmount;

    const transactionReference = input.transactionReference 
    
  
    

    

}

